- Port to non-macOS systems.

- macOS: login items

- macOS: set privacy permissions programatically

- install kontroll once available via homebrew
- think about calling "open" on installed programs so we can configure all of them
